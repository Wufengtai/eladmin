<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="me.zhengjie.modules.goods.mapper.GoodMapper">
    <resultMap id="BaseResultMap" type="me.zhengjie.modules.goods.entity.Good">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="store_id" property="storeId"/>
        <result column="store_name" property="storeName"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="area_id" property="areaId"/>
        <result column="area_name" property="areaName"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="price" property="price"/>
        <result column="specifications" property="specifications"/>
        <result column="warning_num" property="warningNum"/>
        <result column="num" property="num"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, store_id, store_name, user_id, status, price, specifications, warning_num, type, num, create_time, update_time, create_by, update_by, user_name, area_id, area_name
    </sql>

    <select id="findAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from qbs_goods
        <where>
            <if test="criteria.ids != null and criteria.ids.size() > 0">
                and id in
                <foreach collection="criteria.ids" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
            <if test="criteria.name != null and criteria.name != ''">
                and name like concat('%', #{criteria.name}, '%')
            </if>
            <if test="criteria.status != null">
                and status = #{criteria.status}
            </if>
            <if test="criteria.type != null">
                and type = #{criteria.type}
            </if>
            <if test="criteria.createTime != null and criteria.createTime.size() != 0">
                and create_time between #{criteria.createTime[0]} and #{criteria.createTime[1]}
            </if>
        </where>
    </select>
</mapper>